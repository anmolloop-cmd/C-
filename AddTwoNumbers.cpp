#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        // Read the input a, b
        int a, b;
        cin >> a >> b;

        // Compute the ans.
        int ans = a + b;
        cout << ans << "\n";
    }

    return 0;
}